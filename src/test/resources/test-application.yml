spring:
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: none
  datasource:
    url: jdbc:mysql://localhost:3306/giovanni
    username: giovanni
    password: password
s3:
  bucket-name: infinity-songbook.test
aws-access-key: test
aws-secret-key: test